{
  "name": "chirp-jobs",
  "version": "1.0.0",
  "description": "Open-source standalone job board — Node.js, JSON storage, no database.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "seed": "node scripts/seed.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.5.0",
    "nodemailer": "^7.0.3"
  }
}
